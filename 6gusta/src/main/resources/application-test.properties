# =======================
# Nome da aplicação
# =======================
spring.application.name=6gusta

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=luiz.carvalho7110@sounidesc.com.br
spring.mail.password=jtqkdmowtmpcudtm
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=3000
spring.mail.properties.mail.smtp.writetimeout=5000

management.endpoint.health.show-details=always
management.endpoints.web.exposure.include=*


# =======================
# Configuração do Banco H2
# =======================
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# =======================
# Console do H2
# =======================
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# =======================
# Flyway para H2
# =======================
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration/h2
spring.flyway.baseline-on-migrate=true

# =======================
# JPA (Remover ddl-auto pois Flyway cuida das tabelas)
# =======================
# NÃO USE ddl-auto com Flyway para evitar conflitos
# spring.jpa.hibernate.ddl-auto=update ? Removido

# =======================
# Swagger UI
# =======================
springdoc.swagger-ui.path=/swagger-ui.html

# =======================
# Logs extras (opcional)
# =======================
logging.level.org.springdoc=DEBUG
